// Template for a user model
// Implement based on your database choice (MongoDB, PostgreSQL, etc.)
export class User {
    constructor(id, username, email) {
        this.id = id;
        this.username = username;
        this.email = email;
    }
} 